# classify-wrapper.conf — defaults for classify-airports.sh
#
# Sourced as bash. Edit the values to change persistent defaults.
# Both keys are optional; if absent, the hardcoded fallback inside
# classify-airports.sh is used. Pressing Enter at a prompt accepts
# the value here; typing a new path overrides for that run only
# (this file is NOT modified at runtime).

DEFAULT_SOURCE_DIR="/Users/lukehogan/AOA-Code/faa-downloader/airports-2026-05-06"
DEFAULT_DEST_DIR="/Users/lukehogan/Documents/startups/aoa/products/artwork/airports"
